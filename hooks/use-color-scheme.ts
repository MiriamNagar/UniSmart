/**
 * Color scheme hook re-export.
 * 
 * Re-exports React Native's useColorScheme hook for consistent usage
 * across the application. This hook detects the device's color scheme
 * preference (light or dark mode).
 * 
 * @module hooks/use-color-scheme
 */

export { useColorScheme } from 'react-native';
